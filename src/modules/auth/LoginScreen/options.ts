import { NativeStackNavigationOptions } from '@react-navigation/native-stack';
import { baseOptions } from '@root/styles/baseOptions';

export const loginOptions: NativeStackNavigationOptions = {
  ...baseOptions,
  title: 'Login',
};
