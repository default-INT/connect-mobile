import { memo } from 'react';
import { View } from 'react-native';
import { AppText } from '@components/AppText';

export const EventsMapScreen = memo(() => (
  <View>
    <AppText>EventsMapScreen</AppText>
  </View>
));

EventsMapScreen.displayName = 'EventsMapScreen';
