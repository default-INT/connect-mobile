import { StyleSheet } from 'react-native';
import { theme } from '@root/styles/theme';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: theme.neutralTertiary,
    alignItems: 'center',
    justifyContent: 'center',
    overflow: 'hidden',
  },
});
